[build]
  base    = "frontend/admin-dashboard"
  command = "npm run build"
  publish = "dist"          # ← relative to base, NOT the full path

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200